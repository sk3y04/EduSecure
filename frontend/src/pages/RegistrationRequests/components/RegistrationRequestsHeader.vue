<template>
  <section class="page-hero desktop-page-panel hero-shell">
    <div class="max-w-4xl">
      <p class="section-kicker">Registration requests</p>
      <h2 class="section-title">Request access to a space</h2>
      <p class="section-copy max-w-none">
        Enter a space code to request membership. Staff still control approval, and pending requests
        remain visible here until they are reviewed or cancelled. The desktop layout gives history the
        main lane while request entry and status summary stay stacked beside it.
      </p>
    </div>

    <div class="mt-8 grid gap-4 xl:grid-cols-3">
      <div class="surface-panel-muted px-5 py-4">
        <p class="meta-label">Primary history lane</p>
        <p class="mt-2 text-base font-semibold text-[var(--color-heading)]">Request tracking stays prominent</p>
      </div>
      <div class="surface-panel-muted px-5 py-4">
        <p class="meta-label">Fast entry</p>
        <p class="mt-2 text-base font-semibold text-[var(--color-heading)]">Submit new requests from a dedicated panel</p>
      </div>
      <div class="surface-panel-muted px-5 py-4">
        <p class="meta-label">Auditable outcomes</p>
        <p class="mt-2 text-base font-semibold text-[var(--color-heading)]">Review notes and statuses remain visible</p>
      </div>
    </div>
  </section>
</template>

